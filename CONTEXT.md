# rihla-client — Handoff

> Read relevant section only. Appends 3–6 lines. Prune Changelog > 25.
> Last updated: 2026-08-08

## Current status
- Client running on **3100** (`next start`, setsid) with built output; `.env.local` points Core 3000 / Geo 8000.
- All 4 backend services up (Core 3000, Risk 3001 auto, ai 3003, Geo 8000). Repos all merged + pushed.
- Chat/voice/identify response types and message objects now carry `usage` / `providerCalls` / `providerAttempts` telemetry (captured from ai-service, rendered on assistant bubbles).

## In-progress / next
- PROD LIVE-SITE FIX PUSHED (new commit): middleware no longer 307-redirects `/api/*` → `/en/api/*` (was breaking the `/api/:path*` rewrite → every API call 404: login, `/auth/refresh`, `/geo/notice`). Now `isApi` → `NextResponse.next()` (bypasses next-intl). CSP: added `worker-src 'self' blob:` (silences extension worker block) + `img-src https://*.tile.openstreetmap.org https://unpkg.com` (Leaflet tiles + markers). `connect-src` unchanged. PENDING: redeploy + smoke (login, chat SSE, map/nearby). Notifications inbox + context alerts + telemetry shipped; next: verify SSE live-delivery in a real browser.

## Architecture notes
- Full API layer in `src/lib/api/` (coreClient w/ 401 refresh + queue; geoClient no refresh).
- Chat supports SSE (`streamMessage`), voice, identify (multipart + long timeout).
- i18n via `[locale]` route segment + next-intl; middleware routes locale.
- Pages: auth, chat, explore(map), currency, safety, quests(+detail), wallet, profile, onboarding, tickets, payment-result; admin removed (dedicated teammate app).

## Key files
- `src/lib/api/client.ts` (axios + refresh) · `src/lib/api/chat.ts`
- `src/lib/api/notifications.ts` · `src/lib/stores/notification-store.ts` (SSE + inbox)
- `src/components/layout/{top-bar,notification-bell}.tsx` · `src/components/chat/context-alert.tsx`
- `src/app/[locale]/notifications/page.tsx` · `src/components/chat/chat-message.tsx` (telemetry)

## Gotchas
- Port 3000 is Core-Server → run client on 3100 (was 3050) for this session.
- First compile slow (~60-75s) on this filesystem.
- `POST /context-notifications/location` triggers Core→ai-service `/analyze` path (context-engine); ai-service route is mounted at `/analyze` (no `/context` prefix) — kept as-is.
- Notifications SSE uses fetch (Bearer token); bell connects only when authenticated.

## Changelog
- 2026-08-09: Bug-fix + full style modernization pass (25 files). **Fixes**: top-bar hieroglyph animation now self-contained (added inline `@keyframes rihlaHiero`; was relying on globals), removed dead `SUN_ROTATION`/`prettyTitle`/dup `hover:` classes; footer dead `href="#"` links → real GitHub repo + issues links (removed guessed X/email per no-fabricated-URLs rule); Rafiq drawer suggestion chips now clickable → `sendMessage` (was inert spans); `EmptyState` icon-string bug fixed (string icons now map to Lucide icons; was rendering literal "MapPin" text); `bg-navy`/`text-navy` undefined-token bug in notifications page active tabs/toggles → `bg-nile`/`text-limestone`; removed login debug console.logs + fake submit onClick; removed unused `useEffect` (rafiq-launcher), unused `currentConversationId` (chat/[id]), unused `locale` (layout), dead `isPublic`/`publicPaths` (middleware); added `landing.learnMore` i18n en/ar. ✓ Style modernization: `globals.css` — new shadow-sm/md/lg/xl + glow tokens, `--ease-out`/`--ease-spring` curves, saturation glass/glass-card, `card-hover` lift, `hover-lift`/`shadow-*`/`animate-fade-in` utilities, `scrollbar-slim` (theme-aware), global `:focus-visible` ring, smooth-scroll + `prefers-reduced-motion` support, refined `gradient-gold`/`text-gradient`. Modernized top-bar (scroll-aware sticky elevation, nav active pill w/ gradient+glow, hover states), footer (pill social buttons, Account column), mobile-nav (active indicator bar + scale), RihlaButton (hover lift/brightness, active scale, per-variant glow), StatusChip (gradient chip), WebField, landing hero (text-shadow, active-scale CTA), stats (gradient numbers), feature cards (lift + slide-in "Learn more"), cities (lift). Replaced raw `#2E9C93`/`#C8831A` chat/leaderboard/profile hexes with `faience`/`gold` tokens; `settings` toggle off-state → `var(--border)`; sensitive-area `info` tone → faience. tsc + `next build` pass; lint clean except the 2 pre-existing (package-grid immutability, payment-result effect). Deployed in dev on :3000 to verify keyframes/footer/glyphs render.
- 2026-08-09: Context-notifications harden. (1) `notification-store.ts` `reportLocation` now seeds deduped inbox rows → SSE `applyLive` skips already-counted ids, fixing the double-counted unread badge. (2) `connectNotificationStream` gains exponential auto-reconnect (2s→30s) + `disposed` guard; reconnects on stream drop without manual re-trigger. (3) `LocationUpdateData.contextReport` now `ContextReport | null` + `skipped?` flag (matches Core throttle — negligible movement pings return no report). tsc clean; only the 2 pre-existing lint errors remain.
- 2026-08-09: Consistency audit vs Core routes. Fixed: (1) Core `GET /api/geo/notice` restored (had been lost from `geo.routes.ts`/`geo.service.ts` despite 08-07 changelog) → `SensitiveAreaNotice` 404s on every page every 15s are gone; shape `{active,class,severity,distance_meters,guide_key}` via GIS `/api/v1/context`. (2) Dead code removed: `walletApi.confirmPayment` (hit non-existent `/wallet/confirm`), `geoClient` (unused; all geo via `coreClient`), `API_BASE_URLS`, `NEXT_PUBLIC_GEO_API_URL`+`NEXT_PUBLIC_AI_API_URL` env refs (→`.env.example`/`.env.local`/AGENTS.md). (3) Deduped `getCityRisk`/`useCityRisk` (dead dup of `getRiskSummary`/`useRiskSummary`) + shared `coreBaseURL` export (was 3 copies). Core rebuilt + restarted on 3000; `/api/geo/notice` verified (near caution zone → `{active:true,class:"caution",distance_meters:51.74}`, out-of-country → `{active:false}`, bad lat → 400). Client build + pre-existing 2 lint errors only.
- 2026-08-08: PROD LIVE-SITE FIX. Two files: `src/middleware.ts` (`isApi` → `NextResponse.next()`, no longer runs next-intl on `/api/*`, kills `/en/api/...` 404s on login/refresh/geo-notice) + `next.config.ts` CSP (`worker-src 'self' blob:`; `img-src` + `https://*.tile.openstreetmap.org` + `https://unpkg.com`). `next build` passes. Pushed to `Rihla-Ecosystem/WiredClient` main.
- 2026-08-08: Wired the new backend context-notification subsystem into the client. Added `src/lib/api/notifications.ts` (inbox/unread/read-all/delete/reports/preferences/SSE via fetch) + `src/lib/stores/notification-store.ts` (zustand, live stream apply, location reporting). New `/notifications` page (inbox + reports tabs, priority styling, mark-all-read, delete), `NotificationBell` with unread badge in TopBar (auth-gated, 60s poll), `ContextAlert` in chat page that geolocates → reports location → surfaces CRITICAL/HIGH alerts + navigates to inbox. Added usage telemetry types (`UsageResult`, `ProviderCall`, `ProviderAttempt`) to chat API; stream/voice/identify now stash `usage`/`providerCalls`/`providerAttempts` on assistant messages and `chat-message.tsx` renders calls/tokens/retries chips. i18n `notifications.*` en/ar. tsc + next build pass, page 200 on 3100.
- 2026-08-07: Created `AGENTS.md` + `CONTEXT.md` (from package.json + api layer). Not started this session.
- 2026-08-07: Sensitive-area feature: added `geoApi.getAreaNotice` + `AreaNotice` type, `use-area-notice` hook (refetchInterval 15s, enabled on coords), `SensitiveAreaNotice` component (self-geolocates via useEffect, dismissible banner, class-colored severity, mounted in `[locale]/layout.tsx`), i18n `guide.*` en/ar. Client build passes.
- 2026-08-07: Massive design refactor (P1 foundation + P2 shell) started. Adopted Ahmed/Rihla-frontend visual language: rewrote `globals.css` tokens (limestone/basalt/nile/solar/faience/copper + safe/amber/red; old nile/sand/gold/terracotta aliases remapped so existing classes resolve), fonts via Google @import (Cormorant Garamond headings, Inter body, Cairo Arabic), copied `public/logo.png`. New `RihlaGlyph/RihlaLogoFull/Geom/PyramidSkyline` atoms (`src/components/shared/rihla-logo.tsx`). Built `AppShell` (dark collapsible sidebar, Rafiq emphasized faience), `TopBar` (animated gradient+pyramid+particles, location chip, avatar menu), `RafiqDrawer`+`RafiqLauncher` (slide-over chat reusing chat-store/ChatMessage/ChatInput), relabeled `MobileNav` (Rafiq central), new `/settings` route + i18n en/ar, restyled `AuthLayout` w/ logo. Layout now: TopBar > AppShell(sidebar) > main > Footer > MobileNav > overlays. Build passes, running on 3050.
- 2026-08-07: Note: page-level restyle of all 23 routes is NOT complete — most pages already pick up new palette via remapped tokens, but components/colors not yet converted (P3 todo).
- 2026-08-07: P3 shared primitives + key pages done. Added `src/components/ui/rihla-ui.tsx` (RihlaButton solar/faience variants, WebField focused input, StatusChip). Restyled landing hero (radial solar gradient + PyramidSkyline + RihlaButton CTA), chat page header (faience live-dot + title), chat-message (user bubbles faience), chat-input (faience persona active + solar send btn), chat-sidebar (fai new-conv + active), profile-card (faience avatar + dual radial accent). Note: global token remap already maps `gold`→`#C8831A`(light)/`#2E9C93`(dark), so ALL remaining pages' CTAs/accents auto-match the new palette — no per-page hex needed. Build passes, running on 3050.
- 2026-08-07: P4 validation done. Walked all 25 routes (`/en/*` + `/ar/*` + admin + auth): all 200, 0 errors, 0 MISSING_MESSAGE in log. Verified fonts (Cormorant/Inter/Cairo) + logo.png load in HTML; pyramid skyline, Ask Rafiq launcher, solar CTA all render. `npm run lint`: fixed my `app-shell.tsx` display-name error (refactored `makeIcon`→`ShelfIcon` component); remaining 2 lint errors are pre-existing in untouched files (`payment-result/page.tsx` setState-in-effect, `wallet/package-grid.tsx` immutability). Build passes, running on 3050.
- 2026-08-07: Egyptian accent pass on `src/components/profile/`: `profile-card`/`trip-history`/`badges-grid` now use inline faience `#2E9C93` (Avatar/location pins) and solar `#C8831A` (level/XP numbers) instead of remapped `bg-gold`/`text-gold`. `badges-grid.tsx` had no gold accents (already structural tokens only) — unchanged. `next build` compiled + TS passed.
- 2026-08-07: Nav/UX cleanups: (1) Removed `AppShell` sidebar entirely (deleted file); all nav now in `TopBar` with icons (Home/Explore/Rafiq/Safety/Quests/Wallet/Settings on desktop; +Profile in mobile/hamburger). (2) Fixed dark-mode text bug: `.dark` was `--sand:#2A2925` (near-black) but 147 `dark:text-sand` needed legible → now `--sand:#EFE4CD`; safe because every `bg-sand*` already has a `dark:bg-*` override. (3) Notification bell links to `/safety` (had dead `/alert`). (4) Killed 2nd floating bot — deleted `global-bot.tsx` + `location-bot.tsx`; kept single `RafiqLauncher` (desktop+mobile, bottom-right faience, bottom:24). EmergencyContacts (red, bottom-left) + SensitiveAreaNotice (top, dismissible) are NOT bots, retained. Build passes, running on 3050.
- 2026-08-07: Fixed quests i18n params. `quests.stepTitle`/`xpEarned`/`completeAllSteps` used `{{n}}`/`{{theme}}` (double braces) → next-intl escaped-literal/ICU error, fell back to raw `quests.stepTitle` on the quest detail page. Corrected to single braces `{n}`/`{theme}` in `messages/en.json` + `messages/ar.json` (matching the working `{distance}` pattern). Build passes.
- 2026-08-07: TopBar fixes + nav rebalance. (1) Fixed avatar Profile dropdown being invisible: header had `overflow:hidden` clipping it — moved sun/particles/pyramid/horizon decor into their own inset `overflow:hidden` layer, dropdown now renders. (2) Location chip was a dead `<span>` → now a `<Link href="/explore">` ("View on map"). (3) Nav rebalance: main top-bar nav = Home/Explore/Rafiq/Safety/Tickets/Currency/Quests (restored Tickets + Currency with Ticket/Coins icons); Wallet + Settings moved into avatar profile dropdown (Profile/Wallet/Settings/Quests/Admin/Logout) + mirrored in mobile dropdown. Build passes, running on 3050.- 2026-08-07: Removed admin dashboard (teammates have dedicated app). Deleted `[locale]/admin/*`, `admin-guard.tsx`, `components/admin/*`, `lib/api/admin.ts`, `use-admin.ts`; pruned 7 admin methods + `GeoAdminSite*` from `geo.ts` and `useCreateSite/UpdateSite/DeleteSite` from `use-geo.ts`; removed Admin linkblock from TopBar dropdown + `admin` i18n namespace (en+ar). `/en/admin` now 404. Build + lint pass.
- 2026-08-07: Tech-debt + SEO pass. Deleted dead scaffolding: root `page.tsx`, root `globals.css`, `navbar.tsx`, `risk-gauge.tsx`; unused assets `egymonuments.com.json`, `images/{logo,logo-dark}.svg`, `images/destinations/`, `{file,globe,window,next,vercel}.svg`; removed unused deps `framer-motion`, `date-fns`. Added `chat.suggestions` (nearby/hidden/history) to `messages/ar.json` (was missing — Rafiq drawer suggestions empty in Arabic). Added SEO: `sitemap.ts` (en+ar for 7 public paths via NEXT_PUBLIC_SITE_URL), `robots.ts` (disallow auth/onboarding), per-page `metadata.ts` for explore/quests/safety/tickets/chat/currency. All 24 routes 200, `/en/admin` 404, sitemap.xml + robots.txt serve. Client on 3050.
- 2026-08-07: Built 8 of 9 empty 404 route dirs into functional pages. (1) `/auth/verify-email`: `verifyEmail`+`resendVerification` added to authApi, new page (token verify + resend w/ email input), auth.* verify i18n en/ar, metadata noindex. (2) `/leaderboard`: new public leaderboardApi + page (rank/avatar/xp/level), new `leaderboard` i18n ns, added to sitemap. (3) `/memory` + `/memory/summary`: new memoryApi (history/summary), pages using TripHistory, `memory` i18n ns. (4) `/profile/badges`: reuses BadgesGrid. (5) `/profile/journeys`: tabbed (Trip History via memoryApi + Quests via journeysApi). (6) `/chat/[id]`: detail page (setActiveConversation(id)), sidebar rows now router.push to /chat/[id]. (7) `/wallet/purchase/[id]`: package buy page (find package → purchasePackage → Paymob redirect). `/wallet/packages` stays 404 (user decision). Build passes, all 8 routes 200 en+ar, client on 3050.
- 2026-08-07: Fixed Core-Server email links (backend): `sendVerificationEmail`/`sendPasswordResetEmail` now build `${FRONTEND_URL}/${locale}/auth/{verify-email,reset-password}?token=` (were missing `/auth` + locale); `resolveLocale()` helper added; register/resend/forgot pass user language. Rebuilt Core-Server dist + restarted on 3000.
- 2026-08-07: TopBar cleanup. Removed animated pyramid skyline (PyramidSkyline) and the "Your location"/"Get location" chip (incl. its geolocation effect + MapPin/ping animation). Removed now-unused imports (MapPin, Search, Banknote) and unused keyframes (rihlaFloat, rihlaShimmer, rihlaPing). Restored missing `href="/safety"` on the bell link (was consumed by the chip removal). Logo stays in top-left brand block. Build passes, client on 3050.

- 2026-08-07: Footer/ambience pass. globals.css @import → Cairo-only (--font-heading/body/arabic all Cairo) + added Noto Sans Egyptian Hieroglyphs. Replaced all inline Cormorant/Inter fontFamily → "Cairo", sans-serif in top-bar/rafiq-drawer/settings (0 leftovers). Footer: "R" badge → RihlaGlyph(36), Twitter icon → lucide X (label "X (Twitter)"). Favicon: [locale]/layout.tsx icons → /logo.png (icon+apple). TopBar: 7 floating hieroglyph particles (ankh 𓋹, eye 𓂀, scarab 𓆣, falcon 𓅃, star 𓇼, djed 𓊽, lotus 𓆼) via rihlaHiero keyframe (rise+fade, pointer-events none) in decoration layer. Build+lint pass (no new errors, only the 2 pre-existing), client restarted on 3050.
